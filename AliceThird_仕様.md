# AliceThird 仕様書

AliceSecond（Ver 2.10a, 2000年）の26年ぶりの後継エンジン。
スクリプト形式・拡張子（`.alc`）はAliceSecondと互換。

> **オリジナル**: [AliceSecond Ver2.10a — Vector](https://www.vector.co.jp/soft/dl/win95/game/se093675.html)（製作：時津城 克己）

---

## 概要

- **言語**: C# / .NET 8 / WPF（Windows専用）
- **バージョン**: 3.0.0-beta.1
- **スクリプト形式**: テキストファイル（UTF-8 または Shift-JIS）、拡張子 `.alc`
- **起動**: `AliceThird.exe [スクリプトパス]`
  引数省略時は実行ファイルと同じフォルダの `index.alc` を読み込む

---

## ウィンドウ構成

| ウィンドウ | 役割 |
|---|---|
| PIC | 画像・スプライト・動画表示（640×480）|
| TXT | テキスト表示（左下）|
| COM | コマンド（選択肢）表示（右下）|

---

## フォルダ構成

```
ゲームフォルダ/
  index.alc       スタートスクリプト
  alice.ini       設定ファイル（省略可）
  pic/            画像ファイル（BMP, PNG, JPG 等）
  bgm/            BGM音声ファイル（WAV, MP3 等）※ない場合 midi/ を参照
  se/             効果音ファイル（WAV 等）※ない場合 wave/ を参照
  avi/            動画ファイル（AVI, MP4 等）
  plugin/         Susie PlugIn（参照のみ、現バージョンでは未使用）
```

> **後方互換**: `bgm/` がなければ `midi/`、`se/` がなければ `wave/` を自動参照。

---

## 設定ファイル（alice.ini）

スクリプトと同じフォルダに置く。`#` または `;` で始まる行はコメント。

```ini
# テキスト行間ウェイト（ミリ秒）。デフォルト 0（即時表示）
TextDelay=200
```

---

## スクリプト記述ルール

- **1行1命令**
- **1文字目が命令コード**、2文字目以降が引数
- **全角文字で始まる行**はメッセージとして自動改行付きで表示
- 空行は無視
- エンコーディング: UTF-8 優先、失敗時 Shift-JIS（CP932）にフォールバック

---

## 命令一覧

### 基本制御

| 命令 | 書式 | 説明 |
|---|---|---|
| `'` | `'コメント` | コメント行（無視）|
| `Z` | `Z` | スクリプト終了 |
| `+` | `+` | クリック／Enterキー待ち |
| `W` | `Wm` | m ミリ秒ウェイト |
| `w` | `wm` | テキスト行間ウェイトを m ミリ秒に設定（`alice.ini` の値を上書き）|

### ラベル・ジャンプ

| 命令 | 書式 | 説明 |
|---|---|---|
| `:` | `:label` | ラベル定義 |
| `*` | `*label` | label へジャンプ |
| `*` | `*label1:label2:label3` | コロン区切りのラベルからランダムにジャンプ |
| `&` | `&label` | label の行アドレスを X0 に代入 |
| `J` | `Jn` | 変数 Xn の指す行アドレスへジャンプ |

### サブルーチン

| 命令 | 書式 | 説明 |
|---|---|---|
| `{` | `{subname` | サブルーチン subname の定義開始（通常実行時はスキップ）|
| `}` | `}` | サブルーチン終了（呼び出し元に戻る）|
| `\` | `\subname` | サブルーチン subname を呼び出す（スタック最大255段）|

### テキスト表示

| 命令 | 書式 | 説明 |
|---|---|---|
| （全角文字）| `メッセージ` | 改行付きで TXT に表示。行間ウェイトを適用 |
| `#` | `#` | 改行のみ |
| `#` | `#メッセージ` | 改行付きで表示。行間ウェイトを適用 |
| `^` | `^メッセージ` | 改行なしで表示 |
| `~` | `~n` | 変数 Xn の値を改行なしで表示（`‾` も同義）|
| `A` | `Ar g b` | テキスト色を RGB（各 0〜255）で設定 |
| `a` | `ab i` | 太字（b=1）・斜体（i=1）の設定 |
| `f` | `ffontname` | テキストフォントを fontname に設定 |

### コマンド（選択肢）

| 命令 | 書式 | 説明 |
|---|---|---|
| `-` | `-コマンド名:label` | コマンドテーブルに選択肢を追加 |
| `?` | `?` | コマンドを表示し選択待ち。テーブルはクリア |
| `?` | `?cancelLabel` | キャンセル時（ESC・右クリック）に cancelLabel へジャンプ |

**コマンド操作**

| 操作 | 動作 |
|---|---|
| マウス左クリック | 選択肢をクリックして選択 |
| `↑` / `↓` キー | 選択肢を移動 |
| `Enter` / `Space` | 選択を確定 |
| `ESC` / 右クリック | キャンセル（cancelLabel へジャンプ） |

### 変数演算

変数は X0〜X255 の256個（整数）。

| 命令 | 書式 | 説明 |
|---|---|---|
| `I` | `In m` | Xn = m |
| `i` | `in m` | Xn = Xm |
| `P` | `Pn m` | Xn += m |
| `p` | `pn m` | Xn += Xm |
| `M` | `Mn m` | Xn -= m |
| `m` | `mn m` | Xn -= Xm |
| `K` | `Kn m` | Xn = Xn × m |
| `k` | `kn m` | Xn = Xn × Xm |
| `D` | `Dn m` | Xn = Xn ÷ m（ゼロ除算は無視）|
| `d` | `dn m` | Xn = Xn ÷ Xm（ゼロ除算は無視）|
| `C` | `Cn m` | Xn と Xm を比較し X0 に結果を返す（0=等、1=小、2=大）|
| `@` | `@m` | X0 に 0〜m-1 の乱数を代入 |

### 条件分岐

| 命令 | 書式 | 説明 |
|---|---|---|
| `=` | `=n m:label` | Xn == m のとき label へジャンプ |
| `<` | `<n m:label` | Xn < m のとき label へジャンプ |
| `>` | `>n m:label` | Xn > m のとき label へジャンプ |

### フラグ

フラグは 0〜255 番の256個（true/false）。

| 命令 | 書式 | 説明 |
|---|---|---|
| `S` | `Sn` | フラグ n 番をセット |
| `R` | `Rn` | フラグ n 番をリセット |
| `N` | `Nn` | フラグ n 番を反転 |
| `F` | `Fn:label` | フラグ n 番がセットされていれば label へジャンプ |

### 画像表示

| 命令 | 書式 | 説明 |
|---|---|---|
| `.` | `.filename` | `pic/` フォルダの filename を PIC ウィンドウに表示 |
| `$` | `$m1 m2` | PIC ウィンドウを m2 ミリ秒フラッシュ（m1=0:黒、m1=1:白）|

### スプライト

スプライトは 0〜255 番の256個。

| 命令 | 書式 | 説明 |
|---|---|---|
| `L` | `Ln filename` | スプライト n 番に `pic/filename` を読み込む（サイズ自動）|
| `l` | `ln` | スプライト n 番を解放 |
| `O` | `On n1 n2` | スプライト n 番を座標（Xn1, Xn2）に表示 |
| `o` | `on` | スプライト n 番を非表示 |

**透過処理**

| 形式 | 透過の扱い |
|---|---|
| BMP | 純黒（R=0, G=0, B=0）をカラーキーとして透明化（AliceSecond 互換）|
| PNG | アルファチャンネルをそのまま使用 |
| JPG 等 | 透過なし |

> BMP のカラーキー方式は AliceSecond が採用していた旧来の仕様（パレット0番＝黒を透過色とする）を継承したもの。PNG を使用すれば任意形状の透過が可能。

### メディア再生

| 命令 | 書式 | 説明 |
|---|---|---|
| `/` | `/filename` | `bgm/` の filename をループ再生（WAV, MP3 等）|
| `%` | `%filename` | `se/` の filename を再生（WAV 等）|
| `V` | `Vfilename` | `avi/` の filename を再生し終了まで待つ（AVI, MP4 等）|

### ウィンドウ位置

| 命令 | 書式 | 説明 |
|---|---|---|
| `T` | `Tn1 n2 m` | ウィンドウ m を座標（Xn1, Xn2）に移動（m=0:PIC / 1:TXT / 2:COM）|
| `G` | `Gn1 n2 m` | ウィンドウ m の現在座標を Xn1, Xn2 に取得 |

> **実装メモ**: T/G 命令はスクリプト言語仕様として定義されており、インタープリタは正しくパースする。ただし、実際の動作はUI実装依存である。1ウィンドウ構成の AliceThird では無視（no-op）とする。独立した3ウィンドウ実装、あるいはコマンドウィンドウがタイヤ付き物理デバイスであるような特殊実装では有効に機能させること。

### ファイル・外部連携

| 命令 | 書式 | 説明 |
|---|---|---|
| `_` | `_filename` | 別の `.alc` ファイルを読み込み先頭から実行（スクリプト切り替え）|
| `X` | —— | ~~外部 EXE を実行~~ セキュリティ上の理由によりAliceThirdでは廃止 |

### 拡張命令（E命令）

文字列変数は 0〜255 番の256個。

| 命令 | 書式 | 説明 |
|---|---|---|
| `E0000` | `E0000 n` | 文字列変数 n を表示 |
| `E0001` | `E0001 n str` | 文字列変数 n に str を代入 |
| `E0002` | `E0002 n filename` | ファイルの1行目を文字列変数 n に読み込む |

---

## スクリプト例

```
/bgm.wav
.背景.png
主人公は目を覚ました。
+
-起き上がる:okiru
-もう少し寝る:neru
?
:okiru
起き上がることにした。
*end
:neru
もう少し眠ることにした。
:end
+
Z
```

---

## AliceSecond からの変更点

| 項目 | AliceSecond 2.10a | AliceThird 3.0.0 |
|---|---|---|
| BGMフォルダ | `midi/` | `bgm/`（`midi/` フォールバックあり）|
| SEフォルダ | `wave/` | `se/`（`wave/` フォールバックあり）|
| BGM再生 | MIDI専用（mciSendString）| WAV/MP3等（MediaTimeline シームレスループ）|
| 動画 | AVI専用 | AVI/MP4等（WMF対応形式）|
| 画像 | BMP専用＋Susie PlugIn | BMP/PNG/JPG等（WPF対応形式）|
| スプライト透過 | パレット0番（黒）をカラーキーとして透過 | BMP は同方式を継承、PNG はアルファチャンネルをそのまま使用 |
| 入力デバイス | マウス・キーボード・ジョイスティック | マウス・キーボード（ジョイスティックは未対応）|
| キーボード操作 | あり（詳細不明）| 上下キーで選択肢移動、Enter確定 |
| テキストウェイト | なし（CPU速度依存）| `alice.ini` + `w` 命令で設定可能 |
| エンコーディング | Shift-JIS | UTF-8（Shift-JIS フォールバックあり）|
